DirectFB2-eglgbm is built using Meson.

First, configure the build:

  $ meson setup build/

Start the build with:

  $ meson compile -C build/

And install DirectFB2-eglgbm using:

  $ meson install -C build/
